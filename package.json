{
  "name": "ccm-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "Domo custom app prototype.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/copy-domo-assets.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "test:e2e": "npx playwright test --config=tests/e2e/playwright.config.js",
    "domo": "domo",
    "domo:dev": "domo dev"
  },
  "dependencies": {
    "@domoinc/query": "^3.0.0",
    "@domoinc/toolkit": "^2.3.0",
    "ryuu.js": "^6.0.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^8.0.16"
  }
}
